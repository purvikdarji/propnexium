package com.propnexium.controller.web;

/**
 * All role-specific dashboard routes have been migrated to dedicated
 * controllers:
 *
 * /user/** → UserDashboardController
 * /admin/** → AdminDashboardController
 * /agent/** → AgentDashboardController
 *
 * This class is intentionally left empty (no @Controller — routes are owned
 * above).
 */
public class DashboardController {
    // intentionally empty — stubs removed
}
